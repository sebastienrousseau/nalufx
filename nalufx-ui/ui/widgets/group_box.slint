import { NalufxTheme } from "../components/themes/index.slint";

export component GroupBox {
    in property <string> title <=> i-title.text;
    in property <length> spacing;

    layout := VerticalLayout {
        spacing: NalufxTheme.spaces.medium;

        i-title := Text {
            horizontal-alignment: left;
            color: NalufxTheme.palette.white;
            font-size: NalufxTheme.typography.label.size.desktop;
            font-weight: NalufxTheme.typography.label.weight;
        }

        i-layout := HorizontalLayout {
            spacing: root.spacing;

            @children
        }
    }
}