export struct Space {
    small: length,
    medium: length,
    large: length,
}

export global DefaultSpaces {
    in property <Space> space: {
        small: 8px,
        medium: 16px,
        large: 24px,
    };
}